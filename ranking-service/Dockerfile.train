# One-off training job: reads events from SQLite, writes model to shared volume.
# Run: docker compose --profile train run train
FROM python:3.12-slim
WORKDIR /app
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt
COPY train.py .
ENTRYPOINT ["python", "train.py"]
